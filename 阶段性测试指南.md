# LinguaPal 阶段性测试指南

> 适用版本：阶段一（人设与工具）+ 阶段二（英语陪练最小闭环）完成后的仓库状态。
> 目的：让任何人在不读源码的情况下，按本文档判断「当前已实现的功能点是否还能正常工作」。

---

## 0. 当前已实现范围

| 模块 | 实现位置 | 说明 |
| --- | --- | --- |
| 小灵人设 Prompt | `src/server/agent/prompts.ts` | 8 词以内简单英语、最多引入 1 个新词、话题白名单 |
| 发音评估工具 | `src/server/agent/tutorTools.ts` | `pronunciation_evaluate`，返回 0-100 分 + feedback |
| 词汇查询工具 | `src/server/agent/tutorTools.ts` | `vocabulary_lookup`，支持 `word`（具体单词）与 `topic`（话题）两种入参 |
| 词汇掌握记录 | `src/server/agent/tutorTools.ts` | `vocabulary_track`，掌握等级 0-5，进程内存储 |
| 独立工具注册表 | `src/server/agent/tutorTools.ts` | `createTutorToolRegistry()` 只有上述 3 个工具，**不给孩子端任何文件读写权限** |
| 工具护栏 hook | `src/server/index.ts` → `beforeToolCall` | 掌握等级非 0-5 整数 → `rewrite`；发音内容为空 → `block` |
| Agent Loop | `src/server/agent/loop.ts` | `maxTurns=6`、`allow/block/rewrite`、toolResult 回写、事件生命周期 |
| 会话与分支 | `src/server/agent/sessionStore.ts` | JSONL 落盘、`leafId` 分支上下文、简化 compaction |
| 双模型策略 | `src/server/agent/realModel.ts` / `mockModel.ts` | 有 Key 走 `RealModel`；无 Key 走 `MockModel`（离线版小灵） |
| HTTP API + SSE | `src/server/index.ts` | `/api/session`、`/api/reset`、`/api/branch`、`/api/prompt`、`/api/runs` + 事件流 |
| 前端 | `src/client/App.tsx` | 聊天区、Session Tree、Tools 面板、Event Timeline、流式输出 |
| 自动化测试 | `src/server/agent/*.test.ts` | 22 个用例 |

**尚未实现（不要在本轮测试中期待）**：教材知识库 RAG、真实语音采集与第三方语音评测 API、前端评分条组件、家长端。

---

## 1. 环境准备

### 1.1 前置

- Node.js 18+（本仓库验证版本 v22）
- 在仓库根目录执行 `npm install`

### 1.2 两种运行模式（关键，容易踩坑）

| 模式 | 启动方式 | 是否读取 `.env.local` |
| --- | --- | --- |
| 真实模型（RealModel） | `start_teaching_agent.ps1` | 读取 |
| 离线模型（MockModel） | 直接 `npm run teaching-agent:dev` | **不读取**（tsx 不自动加载 `.env.local`） |

`.env.local` 位于仓库根目录（已被 gitignore），变量名：

```text
CW_AGENT_LLM_PROVIDER
CW_AGENT_LLM_BASE_URL
CW_AGENT_LLM_KEY
CW_AGENT_LLM_MODEL
```

启动后看控制台第一行即可确认当前用的是哪个模型：

- 真实模型：`[model] using RealModel with <模型名>`
- 离线模型：`[model] using MockModel (no LLM key configured)`

### 1.3 启动

```powershell
# 真实模型（会打印 [env] loaded .../.env.local）
powershell -ExecutionPolicy Bypass -File .\start_teaching_agent.ps1

# 或：离线模型，不需要任何 Key
npm run teaching-agent:dev
```

| 服务 | 地址 |
| --- | --- |
| 前端 | http://localhost:5174/ |
| 后端 API | http://localhost:4317/ |

---

## 2. 测试分层总览

| 层级 | 目的 | 命令 / 方式 | 预计耗时 |
| --- | --- | --- | --- |
| L1 单元测试 | 保护协议、工具边界、loop 状态机 | `npm run teaching-agent:test` | < 10 秒 |
| L2 类型与构建 | 保证可编译、可打包 | `npm run teaching-agent:typecheck`、`npm run teaching-agent:build` | < 1 分钟 |
| L3 后端 API 冒烟 | 验证 HTTP 链路与工具调用闭环 | 见第 5 节 | 2 分钟 |
| L4 前端走查 | 验证孩子真实可见的交互 | 见第 6 节 | 10 分钟 |
| L5 真实模型冒烟 | 验证三个工具都能被真实模型正确触发 | 见第 7 节 | 10 分钟 |

---

## 3. L1：自动化测试

```bash
npm run teaching-agent:test
```

**通过标准**：末行汇总为

```text
# tests 22
# pass 22
# fail 0
```

### 3.1 22 个用例的覆盖点

| 文件 | 用例数 | 覆盖点 |
| --- | --- | --- |
| `loop.test.ts` | 5 | 工具调用后继续下一轮并产出最终回答；未知工具 → `isError` toolResult；循环调用被 `maxTurns` 拦住；`block` 时工具不执行；`rewrite` 后参数被替换 |
| `mockModel.test.ts` | 5 | 固定英语回复 + 必触发 `pronunciation_evaluate`；非英文输入兜底 `Hello!`；无上下文也能稳定开局；拿到评分后英语收尾并停止；工具报错时引导重试 |
| `tutorTools.test.ts` | 8 | 注册表只暴露 3 个陪练工具；发音评分正常/空输入 0 分；词汇别名与 `availableTopics` 提示；跨话题命中单词；掌握等级被夹取到 0-5；空 word 抛错；掌握等级 helper 边界 |
| `sessionStore.test.ts` | 2 | 按当前 `leafId` 重建上下文、忽略兄弟分支；compaction 记录 `firstKeptEntryId` 并能恢复 |
| `tools.test.ts` | 2 | 文件工具限制在安全工作区内；`../` 路径越界被拒绝 |

> 注意：`tools.test.ts` 覆盖的是教学版通用文件工具（`createToolRegistry`）。**这套工具没有接入陪练注册表**，仅作为单测与教学 Demo 保留。

---

## 4. L2：类型检查与构建

```bash
npm run teaching-agent:typecheck   # 预期：无输出、退出码 0
npm run teaching-agent:build       # 预期：生成 examples/teaching-agent/dist/
```

---

## 5. L3：后端 API 冒烟（离线模式为基线）

先在**离线模式**下启动（保证结果可预测），另开一个 PowerShell 窗口执行：

### 5.1 一键脚本（可直接粘贴）

```powershell
$base = "http://localhost:4317"

# 1) 清空会话，保证基线
Invoke-RestMethod "$base/api/reset" -Method Post | Out-Null

# 2) 工具清单：应恰好 3 个
(Invoke-RestMethod "$base/api/session").tools.name

# 3) 提交一句英语
$r = Invoke-RestMethod "$base/api/prompt" -Method Post -ContentType "application/json" -Body '{"text":"I like dogs."}'

# 4) 最近 4 条消息
$r.messages | Select-Object -Last 4 | ForEach-Object {
  $t = (($_.content | Where-Object { $_.type -eq "text" }).text) -join " / "
  $c = (($_.content | Where-Object { $_.type -eq "toolCall" }) | ForEach-Object { "$($_.name) $($_.arguments | ConvertTo-Json -Compress)" }) -join " "
  "{0} :: {1} {2}" -f $_.role, $t, $c
}

# 5) 工具事件
$r.events | Where-Object { $_.type -like "tool_execution*" } | ForEach-Object { "$($_.type) $($_.toolName) error=$($_.isError)" }
```

### 5.2 预期基线输出（离线模式实测）

```text
pronunciation_evaluate
vocabulary_lookup
vocabulary_track

user :: I like dogs.
assistant :: That's great! 🦊 Can you say it again? pronunciation_evaluate {"transcript":"I like dogs."}
toolResult :: {"accuracy":76,"fluency":90,"completeness":84,"feedback":"再试一次会更好～"}
assistant :: Nice job! Your score is 76. Let's keep going!

tool_execution_start pronunciation_evaluate error=
tool_execution_end pronunciation_evaluate error=False
```

判断要点：

1. 4 条消息顺序必须是 `user → assistant(含 toolCall) → toolResult → assistant`。
2. `toolResult` 里必须有 `accuracy / fluency / completeness / feedback` 四个字段。
3. 最后一条 assistant 必须带上分数，说明 loop 把工具结果回写后**又跑了一轮**（这是「工具调用通常要两轮」的直接证据）。

### 5.3 其余接口

| 接口 | 命令 | 预期 |
| --- | --- | --- |
| `GET /api/session` | `Invoke-RestMethod "$base/api/session"` | 返回 `messages / events / tools / entries / leafId` |
| `POST /api/branch` | `Invoke-RestMethod "$base/api/branch" -Method Post -ContentType "application/json" -Body '{"leafId":"<entry id>"}'` | 返回新上下文；`leafId` 无效 → 400 |
| `POST /api/runs` | `Invoke-RestMethod "$base/api/runs" -Method Post -ContentType "application/json" -Body '{"text":"Hello!"}'` | 返回 `{ runId }`，随后可用 SSE 观察事件 |
| SSE 事件流 | `curl.exe -N "$base/api/runs/<runId>/events"` | 依次输出 user 的 `message_start/end` → `agent_start` → `turn_start` → assistant 的 `message_*` → `tool_execution_start/end` → toolResult 的 `message_*` → `turn_end` → 第二轮 → `agent_end` → `run_done` |
| 空输入 | `-Body '{"text":"  "}'` | HTTP 400 `text is required` |
| 并发提交 | 连续两次快速发起 `/api/prompt` | 第二次返回 HTTP 409 `another run is already active` |

---

## 6. L4：前端走查清单

打开 http://localhost:5174/

| # | 操作 | 预期 |
| --- | --- | --- |
| 1 | 首次打开 | 标题为「🦊 LinguaPal · 小灵」；聊天区显示空态文案「Hi! 我是小灵。用英语和我聊聊吧，比如：I like dogs!」 |
| 2 | 点右上角刷新按钮 | 会话从后端重新拉取，不报错 |
| 3 | 输入框默认值与 placeholder | 默认值 `I like dogs!`；placeholder 为「试试：I like dogs! / What does adopt mean?」 |
| 4 | 发送 `I like dogs!` | 聊天区依次出现 4 条卡片：user、assistant、`tool:pronunciation_evaluate`、assistant |
| 5 | 观察 Event Timeline | 出现 `turn_start` → `message_start/update/end` → `tool_start` → `tool_end` → `turn_end` → `agent_end` |
| 6 | 查看 Tools 面板 | 恰好 3 个工具，且描述为发音评估 / 词汇查询 / 掌握记录（**不应出现 `list_files`、`read_file`、`write_note`**） |
| 7 | 再发送 `The dog is cute.` | 新消息追加，工具事件再次出现，分数根据句子长度变化 |
| 8 | 点 Session Tree 里较早的节点 | 出现 `branch_switch: <leafId>`，聊天上下文回退到该分支，且**不包含兄弟分支内容** |
| 9 | 点重置按钮 | 聊天区回到空态，Session Tree 与 Timeline 清空 |
| 10 | 检查落盘 | `examples/teaching-agent/.teaching-agent/session.jsonl` 有新追加的 entry |

### 6.1 两种模式的行为差异（避免误判为 Bug）

| 场景 | MockModel（离线） | RealModel（真实） |
| --- | --- | --- |
| 孩子说一句英语 | 必触发 `pronunciation_evaluate` | 通常触发 `pronunciation_evaluate` |
| 孩子问 `What does adopt mean?` | **不触发** `vocabulary_lookup`，只走发音评估 | 应触发 `vocabulary_lookup`，参数 `word=adopt` |
| 孩子说 `I learned adopt today.` | **不触发** `vocabulary_track` | 应触发 `vocabulary_track` |
| 孩子说中文 | 兜底按 `Hello!` 评分，不报错 | 模型按人设温和引导用英语再试 |

**结论**：词汇查询与掌握记录的端到端验证必须在**真实模型模式**下做（第 7 节），离线模式只验证发音评估闭环。

---

## 7. L5：真实模型三项冒烟

前置：`.env.local` 已配置，用 `start_teaching_agent.ps1` 启动，控制台确认打印 `[model] using RealModel with ...`。

先在 UI 点一次「重置会话」清干净上下文，然后逐项进行：

| # | 输入 | 期望工具调用 | 期望结果 |
| --- | --- | --- | --- |
| 1 | `I like dogs.` | `pronunciation_evaluate` | 小灵先给分数反馈，再鼓励重说；`toolResult` 含四项评分字段 |
| 2 | `What does adopt mean?` | `vocabulary_lookup`，参数为 `{"word":"adopt"}` | 返回 `adopt` 的释义「领养」和例句，小灵用简单英语解释 |
| 3 | `I learned adopt today.` | `vocabulary_track`，参数含 `word: "adopt"` | `toolResult` 提示已记录词汇，`details.masteryLevel` 在 0-5 之间 |

记录表（建议每次跑完填写）：

| 项 | 是否触发预期工具 | toolResult 是否 isError | 回复是否为简单英语 | 备注 |
| --- | --- | --- | --- | --- |
| 1 发音评估 | ☐ | ☐ | ☐ | |
| 2 词汇查询 | ☐ | ☐ | ☐ | |
| 3 词汇记录 | ☐ | ☐ | ☐ | |

三项均通过，即可认为阶段二的真实模型链路仍然可用。

---

## 8. 工具护栏验证（当前需要手工构造）

`index.ts` 里的 `beforeToolCall` 只在**模型主动给出非法参数**时才会触发，正常对话几乎不会命中。因此护栏需要手工验证：

### 8.1 发音内容为空 → 应被 block

1. 临时修改 `src/server/agent/mockModel.ts`，把

   ```ts
   arguments: { transcript: pickTranscript(last) },
   ```

   改为

   ```ts
   arguments: {},
   ```

2. 发送任意一条消息。
3. 预期：Event Timeline 出现 `tool_permission block: pronunciation_evaluate`；`toolResult` 为 `isError: true`，内容为「Tool call blocked: 缺少待评估的英文内容，请先让孩子用英语说一句。」；工具**没有**真正执行。
4. 验证完把代码改回。

### 8.2 掌握等级非法 → 应被 rewrite

1. 临时把 mock 的 `toolCall` 改为调用 `vocabulary_track`，参数为 `{ word: "adopt", masteryLevel: 99 }`。
2. 发送任意一条消息。
3. 预期：出现 `tool_permission rewrite: vocabulary_track`，`args.masteryLevel` 被改写为 `5`，工具正常执行成功。
4. 验证完把代码改回。

### 8.3 工具层边界（已由单测覆盖）

| 用例 | 预期 |
| --- | --- |
| `pronunciation_evaluate` 传空字符串 | 返回 0 分 + 「我还没听到英文哦」，**不抛异常** |
| `vocabulary_lookup` 传未知单词（如 `photosynthesis`） | 返回 `error: unknown word` + `availableTopics` |
| `vocabulary_lookup` 传未知话题（如 `space travel`） | 返回 `error: unknown topic` + `availableTopics` |
| `vocabulary_track` 不传 word | 抛错 `word is required`，被 loop 包成 `isError` toolResult |
| `vocabulary_track` 传 `masteryLevel: 3.6` | 记录为 `4` |

---

## 9. 已知盲区与待办

| # | 问题 | 影响 | 建议 |
| --- | --- | --- | --- |
| 1 | `index.ts` 的 `beforeToolCall` 具体策略没有直接单测，`loop.test.ts` 只覆盖通用的 block / rewrite 通路 | 护栏策略被改动时可能不会被测试发现 | 把 `beforeToolCall` 抽到独立文件并补 2 个用例，或加一个 `server.test.ts` |
| 2 | MockModel 只触发发音评估 | 离线模式无法演示词汇查询/记录 | 在 MockModel 里按关键词（`What does ... mean?` / `I learned ...`）分流到另两个工具 |
| 3 | 前端没有评分条组件，分数以 JSON 文本呈现 | 孩子看不到直观的分数反馈，影响演示观感 | 阶段四做评分条 UI（`details` 里已经有现成字段） |
| 4 | `vocabulary_track` 记录存在进程内存，重启即清空 | 无法做跨会话的掌握度追踪 | 阶段三/四落盘到 SQLite 或 JSONL |
| 5 | 直接 `npm run teaching-agent:dev` 不读 `.env.local` | 容易误以为配置失效而走成了 Mock | 在启动日志里显式打印当前模型来源 |
| 6 | `.env.local` 里的 `CW_AGENT_LLM_PROVIDER` 只被启动脚本打印，未被 `realModel.ts` 读取 | 切换供应商需要靠 base URL 隐式区分 | 要么在 adapter 里实际使用，要么从 `.env.local` 示例中移除 |
| 7 | 文档漂移：`docs/project/build-02-loop-model.md`、`docs/project/testing.md`、`docs/project/run.md` 仍在描述文件工具版 MockModel 与推荐 prompt | 按文档操作会得到与本文档不一致的预期 | 单独一个 commit 收口文档 |

---

## 10. 验收清单

阶段二功能点是否还健康，勾完下面 6 项即可：

- [ ] `npm run teaching-agent:test` → `# pass 22`、`# fail 0`
- [ ] `npm run teaching-agent:typecheck` → 0 error
- [ ] 离线模式发出 `I like dogs.` → 4 条消息链路正确，且最后一条 assistant 带分数
- [ ] 前端 Tools 面板只显示 3 个陪练工具，无文件工具
- [ ] 真实模型三项冒烟（发音评估 / 词汇查询 / 词汇记录）全部触发预期工具
- [ ] Session Tree 分支切换后上下文正确回退，`.teaching-agent/session.jsonl` 正常追加
